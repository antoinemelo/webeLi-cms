{
  "contract": "public.catalog.brands.index.v1",
  "kind": "schema",
  "summary": "Lister les marques publiques du catalogue.",
  "description": "Retourne les marques actives, publiques et non archivées du module Business.",
  "method": "GET",
  "path": "/api/v1/catalog/brands",
  "source": "business_product_brands",
  "security": "Public read endpoint; may be protected by bearer scope catalog:read or headless:read.",
  "query_params": {
    "lang": {"type": "string", "required": false}
  },
  "response": {
    "status": 200,
    "schema": {
      "type": "object",
      "required": ["data", "meta"],
      "properties": {
        "data": {
          "type": "object",
          "required": ["items"],
          "properties": {
            "items": {
              "type": "array",
              "items": {"type": "object", "required": ["id", "name", "slug"], "additionalProperties": true}
            }
          }
        },
        "meta": {"$ref": "#/components/schemas/PublicMeta"}
      }
    }
  },
  "errors": [],
  "examples": {
    "response": {"data": {"items": [{"id": 1, "name": "Acme", "slug": "acme"}]}, "meta": {"contract": "public.catalog.brands.index.v1"}}
  }
}
